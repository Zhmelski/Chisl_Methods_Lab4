#pragma once

#include <iostream>
#include <iomanip>
#include <cmath>
#include <vector>
#include <Windows.h>

using namespace std;

vector<double> GaussMethod(const vector<vector<double>>& A, const vector<double>& b);